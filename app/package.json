{
  "name": "donut-frontend",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.10.1",
    "jsdom": "^24.1.3",
    "typescript": "^5.7",
    "vite": "^6.3",
    "vitest": "^4.1.2"
  }
}
